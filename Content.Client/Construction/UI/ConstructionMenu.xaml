<DefaultWindow xmlns="https://spacestation14.io">
    <!-- #Misfits Fix: Rebalanced panel ratios 0.4/0.6 → 0.5/0.5 so the recipe list and
         search bar are not squished. Reduced Category dropdown MinSize 130 → 100. -->
    <!-- #Misfits Tweak: Added MinSize="190 0" to the left panel so the recipe ItemList
         is never narrower than its longest entry — text will no longer get cut off. -->
    <BoxContainer Orientation="Horizontal" HorizontalExpand="True">
        <BoxContainer Orientation="Vertical" HorizontalExpand="True" SizeFlagsStretchRatio="0.5" MinSize="190 0">
            <BoxContainer Orientation="Horizontal" HorizontalExpand="True">
                <LineEdit Name="SearchBar" PlaceHolder="Search..." HorizontalExpand="True"/> <!-- Corvax-Change -->
                <OptionButton Name="Category" Access="Public" MinSize="100 0"/>
            </BoxContainer>
            <!-- #Misfits Add: "Craftable Now" shows material recipes the player can craft from items
                 currently in hands, bags, or on the ground — no scrolling required. -->
            <BoxContainer Name="CraftableNowSection" Orientation="Vertical" HorizontalExpand="True" VerticalExpand="True" SizeFlagsStretchRatio="1.5" Visible="False">
                <Label Text="Craftable Now" StyleClasses="LabelBig" HorizontalAlignment="Center" Margin="0 4 0 2"/>
                <ItemList Name="CraftableRecipes" Access="Public" SelectMode="Single" MinSize="0 70" VerticalExpand="True"/>
                <Control MinSize="0 6"/>
            </BoxContainer>
            <ItemList Name="Recipes" Access="Public" SelectMode="Single" VerticalExpand="True" SizeFlagsStretchRatio="3"/>
        </BoxContainer>
        <Control MinSize="10 0"/>
        <BoxContainer Orientation="Vertical" HorizontalExpand="True" SizeFlagsStretchRatio="0.5">
            <Control MaxSize="2000 80">
                <BoxContainer Orientation="Horizontal" Align="Center">
                    <TextureRect Name="TargetTexture" HorizontalAlignment="Right" Stretch="Keep"/>
                    <Control MinSize="10 0"/>
                    <BoxContainer Orientation="Vertical">
                        <RichTextLabel Name="TargetName"/>
                        <RichTextLabel Name="TargetDesc"/>
                    </BoxContainer>
                </BoxContainer>
            </Control>
            <ItemList Name="RecipeStepList" Access="Public" VerticalExpand="True"/>
            <Label Text="{Loc 'hand-craft-intell-leftover-materials-title'}" HorizontalAlignment="Center" Margin="0 4 0 2"/>
            <ItemList Name="LeftoverMaterialsList" Access="Public" MinSize="0 70"/>
            <BoxContainer Orientation="Vertical">
                <Button Name="BuildButton" Disabled="True" ToggleMode="True" VerticalExpand="True" SizeFlagsStretchRatio="0.5"/>
                <BoxContainer Orientation="Horizontal" VerticalExpand="True" SizeFlagsStretchRatio="0.5">
                    <Button Name="EraseButton" ToggleMode="True" HorizontalExpand="True" SizeFlagsStretchRatio="0.7"/>
                    <Button Name="ClearButton" HorizontalExpand="True" SizeFlagsStretchRatio="0.3"/>
                </BoxContainer>
            </BoxContainer>
        </BoxContainer>
    </BoxContainer>
</DefaultWindow>
